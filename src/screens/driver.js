import React from 'react';
import { CContent, CSpace, CTitle, CButton } from '@components';

const ScreenDriver = () => {
  return (
    <CContent title="Code de tournée" fullscreen center>
      <CSpace />
    </CContent>
  );
};

export default ScreenDriver;
