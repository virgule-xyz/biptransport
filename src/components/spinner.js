import React from 'react';
import { Spinner } from 'native-base';

/**
 * Just a wrapper around a Native Base spinner
 */
const CSpinner = () => <Spinner color="#3333aa" />;

export default CSpinner;
